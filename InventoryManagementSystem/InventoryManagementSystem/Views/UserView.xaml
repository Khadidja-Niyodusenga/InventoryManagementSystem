<UserControl x:Class="InventoryManagementSystem.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryManagementSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TITLE -->
        <TextBlock Text="Manage Users"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- TOP ACTION BAR -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">
            <TextBox Width="200"
                     Height="30"
                     Margin="0,0,10,0"
                     x:Name="txtSearch"
                    />

            <Button Content="Search"
                    Width="80"
                    Margin="0,0,10,0"
                    Cursor="Hand"
                    Click="Search_Click"
                    />

            <Button Content="Add User"
                    Width="100"
                    Background="#4CAF50"
                    Foreground="White"
                    Margin="0,0,10,0"
                    Cursor="Hand"
                    Click="AddUser_Click"/>

            <Button Content="Edit"
                    Width="80"
                    Background="#2196F3"
                    Foreground="White"
                    Margin="0,0,10,0"
                    Cursor="Hand"
                    Click="EditUser_Click"/>

            <Button Content="Deactivate"
                    Width="80"
                    Background="#F44336"
                    Foreground="White"
                    Margin="0,0,10,0"
                    Cursor="Hand"
                    Click="DeleteUser_Click"/>

            <Button Content="Refresh"
                    Width="80"
                    Background="#6c757d"
                    Foreground="White"
                    Cursor="Hand"
                    Click="Refresh_Click"
                    />
        </StackPanel>

        <!-- DATA TABLE -->
        <DataGrid Grid.Row="2"
                  x:Name="UsersDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="UserDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User ID" 
                                    Binding="{Binding UserID}" 
                                    Width="80"/>

                <DataGridTextColumn Header="Name" 
                                    Binding="{Binding Name}" 
                                    Width="200"/>

                <DataGridTextColumn Header="Department" 
                                    Binding="{Binding Department}" 
                                    Width="150"/>

                <DataGridTextColumn Header="Status" 
                                    Width="80">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsActive" Converter="{StaticResource ActiveStatusConverter}"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>